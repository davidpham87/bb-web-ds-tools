{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to the BB Web DS Tools Wiki","text":"<p>This wiki contains documentation for the BB Web DS Tools application.</p>"},{"location":"#contents","title":"Contents","text":"<ul> <li>App DB</li> <li>Changelog</li> <li>CLI Tools</li> <li>Code Environments</li> <li>Datasets</li> <li>Gemma</li> <li>HoneySQL</li> <li>Malli</li> <li>Vega Lite</li> </ul>"},{"location":"app_db/","title":"App DB View","text":""},{"location":"app_db/#why","title":"Why?","text":"<p>Because you broke the state again, didn't you? It's okay. We all do it. This view is your confession booth, where you can stare directly into the soul of your application and realize that <code>nil</code> is not, in fact, a valid map key, no matter how much you wish it were.</p> <p>The App DB View is a powerful debugging and introspection tool designed for developers. It provides a direct window into the <code>re-frame</code> application database (<code>app-db</code>), which holds the entire client-side state of the application.</p>"},{"location":"app_db/#features","title":"Features","text":"<ul> <li>Path Watching: Subscribe to specific paths within the state tree to   monitor changes in real-time.</li> <li>Portal Integration: Inspect state values using the rich data visualization   capabilities of Portal.</li> <li>History/Diffs: (If enabled) Track how the state changes over time.</li> </ul>"},{"location":"app_db/#step-by-step-examples","title":"Step-by-Step Examples","text":""},{"location":"app_db/#1-inspecting-global-datasets","title":"1. Inspecting Global Datasets","text":"<p>To see what datasets are currently loaded in memory:</p> <p>Steps:</p> <ol> <li>Navigate to the App DB view.</li> <li>In the \"Watch Path\" input (sidebar), type or select the path where datasets     are stored.<ul> <li>Typically: <code>[:bb-web-ds-tools.views.datasets/datasets]</code> or similar,   depending on the internal key structure.</li> </ul> </li> <li>Click Add Watch.</li> <li>The right-hand Portal panel will display the current value of that path.</li> <li>Expand the map to see the details of loaded datasets (rows, metadata, etc.).</li> </ol>"},{"location":"app_db/#2-monitoring-user-input","title":"2. Monitoring User Input","text":"<p>To debug a form input issue:</p> <p>Steps:</p> <ol> <li>Add a watch for the user input path, e.g., <code>[:user-input]</code>.</li> <li>Open the application in a separate window or split view (if available).</li> <li>Type into a text field in another view (e.g., the Malli editor).</li> <li>Observe the <code>[:user-input]</code> value updating in the App DB view in real-time.</li> </ol>"},{"location":"app_db/#3-runtime-state","title":"3. Runtime State","text":"<p>To check the status of background workers (Pyodide, WebR, SCI):</p> <p>Steps:</p> <ol> <li>Add a watch for <code>[:runtime]</code>.</li> <li>Inspect the resulting map to see keys like <code>:sci</code>, <code>:pyodide</code>, or <code>:webr</code>.</li> <li>You can verify flags like <code>:ready?</code> or <code>:loading?</code> to diagnose     initialization issues.</li> </ol>"},{"location":"changelog/","title":"Changelog View","text":""},{"location":"changelog/#why","title":"Why?","text":"<p>So you can reminisce about the bugs we used to have before we replaced them with new, more exciting bugs. It's a historical record of our failures and occasional triumphs, mostly failures. Read it and weep.</p> <p>The Changelog View provides a chronological record of changes made to the application, helping users stay informed about new features, improvements, and bug fixes.</p>"},{"location":"changelog/#features","title":"Features","text":"<ul> <li>Categorized Entries: Updates are grouped by type (e.g., <code>Added</code>,   <code>Changed</code>, <code>Fixed</code>) for clarity.</li> <li>Version Tracking: Each release is clearly versioned and dated.</li> <li>Linkable: Specific versions or changes may be linked (if supported) for   reference.</li> </ul>"},{"location":"changelog/#usage","title":"Usage","text":"<ol> <li>Navigate to the Changelog view via the main navigation menu or footer     link.</li> <li>Scroll through the timeline to review past updates.</li> <li>Newest changes appear at the top.</li> </ol>"},{"location":"cli_tools/","title":"CLI Tools Documentation","text":""},{"location":"cli_tools/#why","title":"Why?","text":"<p>Because using a mouse is for casuals. Real engineers prefer to type obscure commands into a black box and pretend to understand the error messages. This is for when you want to look busy in a coffee shop without actually accomplishing anything visible.</p> <p>The application exposes its core logic as a suite of Command Line Interface (CLI) tools, powered by Babashka. These tools allow you to perform data processing, schema validation, and SQL generation directly from your terminal, enabling automation and scripting.</p>"},{"location":"cli_tools/#installation-setup","title":"Installation / Setup","text":"<p>Ensure you have <code>bb</code> (Babashka) installed on your path. The entry point is the <code>src/dstools.clj</code> script.</p> <p>Execution Wrapper:</p> <pre><code>alias dstools=\"bb -m dstools\"\n</code></pre>"},{"location":"cli_tools/#tools-overview","title":"Tools Overview","text":"<p>All commands follow the pattern: <code>dstools &lt;namespace&gt; &lt;command&gt; [options]</code></p>"},{"location":"cli_tools/#1-datasets-data","title":"1. Datasets (<code>data</code>)","text":"<p>Manage and convert tabular data between formats (CSV, JSON, EDN, Markdown) and structures (Row Maps, Columnar, Row Arrays).</p> <p>Command: <code>convert</code></p> <p>Features:</p> <ul> <li>Format Conversion: e.g., JSON -&gt; CSV.</li> <li>Structure Reshaping: e.g., Row Maps -&gt; Columnar.</li> <li>Automatic Inference: Guesses input format and structure if not specified.</li> </ul> <p>Options:</p> <ul> <li><code>-f, --format &lt;fmt&gt;</code>: Input format (<code>csv</code>, <code>json</code>, <code>edn</code>, <code>yaml</code>).</li> <li><code>-t, --to &lt;fmt&gt;</code>: Output format (<code>csv</code>, <code>json</code>, <code>edn</code>, <code>yaml</code>).</li> <li><code>-i, --file &lt;file&gt;</code>: Input file path (defaults to stdin).</li> <li><code>-o, --out &lt;file&gt;</code>: Output file path (defaults to stdout; infers extension if   input provided).</li> <li><code>-S, --input-struct &lt;struct&gt;</code>: Input structure (<code>row-maps</code>, <code>columnar</code>,   <code>rows</code>).</li> <li><code>-s, --output-struct &lt;struct&gt;</code>: Output structure (<code>row-maps</code>, <code>columnar</code>,   <code>rows</code>).</li> </ul> <p>Examples:</p> <ol> <li> <p>Convert JSON to EDN (Row Maps to Columnar)</p> <p><code>bash dstools data convert -i data.json -t edn -s columnar</code></p> </li> <li> <p>Convert CSV to Markdown Table</p> <p><code>bash dstools data convert -i data.csv -t markdown</code></p> </li> </ol>"},{"location":"cli_tools/#2-honeysql-sql","title":"2. HoneySQL (<code>sql</code>)","text":"<p>Compile HoneySQL data structures into standard SQL.</p> <p>Command: <code>convert</code></p> <p>Features:</p> <ul> <li>Compilation: Transforms <code>.edn</code> files containing Clojure maps into <code>.sql</code>   files.</li> </ul> <p>Options:</p> <ul> <li><code>-i, --file &lt;file&gt;</code>: Input file path containing the HoneySQL map.</li> <li><code>-o, --out &lt;file&gt;</code>: Output file path.</li> </ul> <p>Example:</p> <ol> <li> <p>Compile a Query</p> <ul> <li>Input (<code>query.edn</code>): <code>{:select [:*] :from [:users]}</code></li> <li>Command:</li> </ul> <p><code>bash   dstools sql convert -i query.edn</code></p> <ul> <li>Output (<code>query.sql</code>): <code>SELECT * FROM users</code></li> </ul> </li> </ol>"},{"location":"cli_tools/#3-malli-schema","title":"3. Malli (<code>schema</code>)","text":"<p>Work with Malli schemas for validation, inference, and generation.</p> <p>Commands:</p> <ul> <li><code>infer</code>: Generate a schema from a dataset file.</li> <li><code>generate</code>: Create random data based on a schema file.</li> <li><code>validate</code>: Check if a dataset conforms to a schema.</li> </ul> <p>Options (Common):</p> <ul> <li><code>-i, --file &lt;file&gt;</code>: Input file (data for <code>infer</code>/<code>validate</code>, schema for   <code>generate</code>).</li> <li><code>-s, --schema &lt;file&gt;</code>: Schema file (required for <code>validate</code>).</li> </ul> <p>Examples:</p> <ol> <li> <p>Infer Schema from JSON</p> <p><code>bash dstools schema infer -i data.json &gt; schema.edn</code></p> </li> <li> <p>Generate Data from Schema</p> <p><code>bash dstools schema generate -i schema.edn --samples 10 --format json</code></p> </li> <li> <p>Validate Data</p> <p><code>bash dstools schema validate -i data.json -s schema.edn</code></p> </li> </ol>"},{"location":"code_environments/","title":"Code Environments","text":""},{"location":"code_environments/#why","title":"Why?","text":"<p>Because you clearly can't decide which language is superior, so we gave you all of them to fail in simultaneously. Whether you're a Pythonista, a recovering R user, or a Clojure purist who thinks syntax is for the weak, we have a padded cell waiting for you.</p> <p>The Code View acts as a unified polyglot notebook environment. It integrates multiple language runtimes (Clojure, Python, R) directly in the browser, all connected to a shared Portal inspector for rich data visualization.</p>"},{"location":"code_environments/#features","title":"Features","text":"<ul> <li>Polyglot Runtimes: Switch between Clojure (SCI), Python (Pyodide), and R   (WebR) without leaving the page.</li> <li>Integrated Data Inspector: All evaluation results are sent to a persistent   Portal pane on the right, allowing for deep exploration of nested data   structures, tables, and charts.</li> <li>Dataset Access: Access the global datasets (loaded via the Datasets view)   from within any runtime.</li> <li>Rich Output: Support for rendering text, EDN, JSON, images, and plots.</li> </ul>"},{"location":"code_environments/#step-by-step-examples","title":"Step-by-Step Examples","text":""},{"location":"code_environments/#1-clojure-repl-sci","title":"1. Clojure REPL (SCI)","text":"<p>The Clojure environment uses the Small Clojure Interpreter (SCI).</p> <p>Steps:</p> <ol> <li>Navigate to the Code view.</li> <li>Select the Clojure tab.</li> <li> <p>Paste the following code to manipulate our example data:</p> <p>```clojure (def data   [{:id 1 :score 12.5 :category \"a\"}    {:id 2 :score 10.2 :category \"b\"}    {:id 3 :score 8.7  :category \"c\"}])</p> <p>;; Calculate average score (let [scores (map :score data)]   (/ (reduce + scores) (count scores))) ```</p> </li> <li> <p>Press Ctrl+Enter (or Cmd+Enter).</p> </li> <li>The result (<code>10.466...</code>) will appear in the Portal inspector on the right.</li> </ol>"},{"location":"code_environments/#2-python-pyodide","title":"2. Python (Pyodide)","text":"<p>The Python environment runs Pyodide (Python compiled to WASM). It includes standard libraries and scientific packages.</p> <p>Steps:</p> <ol> <li>Select the Python tab.</li> <li>(Optional) Wait for the runtime to initialize (look for the \"Ready\"     indicator).</li> <li> <p>Execute the following code:</p> <p>```python import pandas as pd</p> </li> <li> <p>Press Ctrl+Enter.</p> </li> <li>A rich table representation of the dataframe summary will appear in Portal.</li> </ol>"},{"location":"code_environments/#define-the-example-dataframe","title":"Define the example dataframe","text":"<p>df = pd.DataFrame([     {\"id\": 1, \"score\": 12.5, \"category\": \"a\"},     {\"id\": 2, \"score\": 10.2, \"category\": \"b\"},     {\"id\": 3, \"score\": 8.7,  \"category\": \"c\"} ])</p>"},{"location":"code_environments/#calculate-summary-statistics","title":"Calculate summary statistics","text":"<p>df.describe() ```</p>"},{"location":"code_environments/#3-r-webr","title":"3. R (WebR)","text":"<p>The R environment uses WebR (R compiled to WASM).</p> <p>Steps:</p> <ol> <li>Select the R tab.</li> <li>(Optional) Wait for the runtime to initialize.</li> <li> <p>Execute the following R code:</p> <p>```r</p> </li> <li> <p>Press Ctrl+Enter.</p> </li> <li>The summary text output will be displayed in Portal.</li> </ol>"},{"location":"code_environments/#create-the-dataframe","title":"Create the dataframe","text":"<p>df &lt;- data.frame(   id = c(1, 2, 3),   score = c(12.5, 10.2, 8.7),   category = c(\"a\", \"b\", \"c\") )</p>"},{"location":"code_environments/#perform-a-linear-model-trivial-example","title":"Perform a linear model (trivial example)","text":"<p>summary(lm(score ~ id, data = df)) ```</p>"},{"location":"code_environments/#4-accessing-global-datasets","title":"4. Accessing Global Datasets","text":"<p>If you have imported a dataset named <code>example-data</code> in the Datasets view, you can access it in the runtimes.</p> <p>Clojure:</p> <pre><code>(get @user/datasets \"example-data\")\n</code></pre> <p>Python (via PyProxy): Accessing shared state requires specific interop calls depending on the implementation.</p> <p>R: Accessing shared state requires specific interop calls depending on the implementation.</p>"},{"location":"datasets/","title":"Datasets View","text":""},{"location":"datasets/#why","title":"Why?","text":"<p>Because manual data entry is a form of torture banned by the Geneva Convention. You're here because you have data in a format that looks like it was generated by a drunk typewriter, and you need it to look like a respectable table before your boss asks why the Q3 projections are written in Sanskrit.</p> <p>The Datasets View is the central hub for managing tabular data within the application. It provides tools to import, visualize, edit, and export datasets in various formats (CSV, JSON, EDN, Markdown). Imported datasets are stored in the application state and can be accessed by other views, such as Vega-Lite for visualization or the Code environments (Clojure, Python, R) for analysis.</p>"},{"location":"datasets/#features","title":"Features","text":"<ul> <li>Multi-Format Import: Support for CSV, TSV, JSON, EDN, and Markdown tables.</li> <li>Structure Normalization: Automatically converts various data shapes   (row-maps, columnar, row-arrays) into a unified internal format.</li> <li>Data Editing: Inline editing of cell values.</li> <li>Data Exploration: Sortable and filterable table view with pagination.</li> <li>Dataset Management: Rename, delete, and switch between multiple active   datasets.</li> <li>Export: Copy data to clipboard or save in different formats.</li> </ul>"},{"location":"datasets/#dataset-structure","title":"Dataset Structure","text":"<p>The application standardizes on Row Maps for internal processing, but supports importing and exporting three common structures:</p> <ol> <li>Row Maps: A sequence of maps, where keys are column names.<ul> <li>Default for JSON/EDN.</li> <li><code>[{:col1 1} {:col1 2}]</code></li> </ul> </li> <li>Columnar: A map of vectors, where keys are column names and values are     columns.<ul> <li>Default for CSV/TSV/Markdown.</li> <li><code>{:col1 [1 2]}</code></li> </ul> </li> <li>Row Arrays: A sequence of vectors, with the first vector as the header.<ul> <li><code>[[\"col1\"] [1] [2]]</code></li> </ul> </li> </ol>"},{"location":"datasets/#step-by-step-examples","title":"Step-by-Step Examples","text":""},{"location":"datasets/#1-loading-data","title":"1. Loading Data","text":"<p>You can import data by pasting text or selecting a file. The system will attempt to infer the format, but you can also specify it manually.</p> <p>Example Dataset: We will use the standard example dataset with columns: <code>id</code>, <code>score</code>, <code>category</code>, and <code>date</code>.</p> <p>JSON Format:</p> <pre><code>[\n  { \"id\": 1, \"score\": 12.5, \"category\": \"a\", \"date\": \"2023-01-01\" },\n  { \"id\": 2, \"score\": 10.2, \"category\": \"b\", \"date\": \"2023-01-02\" },\n  { \"id\": 3, \"score\": 8.7, \"category\": \"c\", \"date\": \"2023-01-03\" },\n  { \"id\": 4, \"score\": 15.0, \"category\": \"a\", \"date\": \"2023-01-04\" },\n  { \"id\": 5, \"score\": 9.9, \"category\": \"b\", \"date\": \"2023-01-05\" }\n]\n</code></pre> <p>CSV Format:</p> <pre><code>id,score,category,date\n1,12.5,a,2023-01-01\n2,10.2,b,2023-01-02\n3,8.7,c,2023-01-03\n4,15.0,a,2023-01-04\n5,9.9,b,2023-01-05\n</code></pre> <p>Steps:</p> <ol> <li>Navigate to the Datasets view.</li> <li>Click the Add Dataset button (or Import tab).</li> <li>In the editor, paste the example JSON or CSV data above.</li> <li>Select the Format dropdown (e.g., <code>:json</code> or <code>:csv</code>).</li> <li>(Optional) Select the Structure if the default inference is incorrect     (e.g., <code>:row-maps</code> for JSON, <code>:columnar</code> for CSV).</li> <li>Click Import.</li> <li>The new dataset will appear in the sidebar list, and the table will populate     with the data.</li> </ol>"},{"location":"datasets/#2-renaming-a-dataset","title":"2. Renaming a Dataset","text":"<ol> <li>In the sidebar, find the dataset you just imported (it will have a default     name like \"Dataset 1\").</li> <li>Click the Edit (pencil) icon next to the name.</li> <li>Type a new name, e.g., <code>example-data</code>.</li> <li>Press Enter or click the checkmark to save.</li> </ol>"},{"location":"datasets/#3-editing-data","title":"3. Editing Data","text":"<ol> <li>Locate a cell in the table you wish to change (e.g., change the score of id     1 from <code>12.5</code> to <code>20.0</code>).</li> <li>Double-click the cell.</li> <li>Enter the new value.</li> <li>Press Enter or click away to commit the change.</li> </ol>"},{"location":"datasets/#4-filtering-and-sorting","title":"4. Filtering and Sorting","text":"<p>Sorting:</p> <ol> <li>Click on the column header <code>score</code>.</li> <li>The table will toggle between Ascending (<code>\u25b2</code>) and Descending (<code>\u25bc</code>) order.</li> </ol> <p>Filtering:</p> <p>The filter inputs accept arbitrary Clojure expressions, which are evaluated using SCI (Small Clojure Interpreter).</p> <ol> <li>Exact Match: Type a value like <code>10</code> or <code>\"a\"</code> (strings must be quoted for     Clojure evaluation, but unquoted strings fall back to equality checks).<ul> <li>Example: <code>12.5</code> matches rows where the column value is <code>12.5</code>.</li> <li>Example: <code>a</code> matches rows where the column value is <code>\"a\"</code> (fallback behavior).</li> </ul> </li> <li>Predicates: Type a function literal to filter values.<ul> <li>Example: <code>#(&gt; % 10)</code> matches rows where the value is greater than 10.</li> <li>Example: <code>even?</code> matches rows where the value is even.</li> <li>Example: <code>#(str/starts-with? % \"2023\")</code> for date strings.</li> </ul> </li> </ol>"},{"location":"datasets/#5-exporting-data","title":"5. Exporting Data","text":"<ol> <li>With the <code>example-data</code> dataset active, click the Export button/tab.</li> <li>Choose your desired output format (e.g., <code>:edn</code> or <code>:markdown</code>).</li> <li>Choose the desired structure (e.g., <code>:columnar</code>).</li> <li>Copy the generated text from the output editor.</li> </ol>"},{"location":"gemma/","title":"Gemma View","text":""},{"location":"gemma/#why","title":"Why?","text":"<p>Because talking to real humans is exhausting, overrated, and often requires \"pants.\" Gemma is here to validate your poor coding decisions and generate hallucinatory facts with absolute confidence, all without leaving your browser or judging your search history.</p> <p>The Gemma View demonstrates the capability of running Large Language Models (LLMs) entirely within the browser using WebGPU and the MediaPipe GenAI Tasks API. It specifically targets the Google Gemma model (e.g., Gemma 2B).</p>"},{"location":"gemma/#features","title":"Features","text":"<ul> <li>Client-Side Inference: No server-side processing; the model runs locally   on your device.</li> <li>WebGPU Acceleration: Utilizes the GPU for efficient performance.</li> <li>Chat Interface: A familiar conversational UI for interacting with the   model.</li> </ul>"},{"location":"gemma/#prerequisites","title":"Prerequisites","text":"<ul> <li>A browser with WebGPU support (e.g., recent Chrome/Edge).</li> <li>A compatible GPU.</li> <li>The model weights file (e.g., <code>gemma-2b-it-gpu-int4.bin</code>) must be available to   the application (usually served from the root).</li> </ul>"},{"location":"gemma/#step-by-step-usage","title":"Step-by-Step Usage","text":""},{"location":"gemma/#1-load-the-model","title":"1. Load the Model","text":"<ol> <li>Navigate to the Gemma view.</li> <li>If the model is not yet loaded, you will see a \"Load Model\" button or a     loading indicator.</li> <li>Click the button to initiate the download and initialization.<ul> <li>Note: This downloads a large file (~1.5GB) and may take some time   depending on your connection.</li> </ul> </li> </ol>"},{"location":"gemma/#2-chatting","title":"2. Chatting","text":"<ol> <li>Once initialized, the chat input field at the bottom becomes active.</li> <li>Type a prompt related to data science or coding. For example:     &gt; \"Explain the difference between a list and a vector in Clojure.\"</li> <li>Press Enter.</li> <li>The model will stream its response into the chat history window.</li> </ol>"},{"location":"gemma/#3-coding-assistance","title":"3. Coding Assistance","text":"<p>You can use Gemma to help write code for other views.</p> <p>Example Prompt:</p> <p>\"Generate a JSON dataset with 5 rows containing id, score, and category fields.\"</p> <p>Expected Output:</p> <pre><code>[\n  { \"id\": 1, \"score\": 85, \"category\": \"A\" },\n  { \"id\": 2, \"score\": 90, \"category\": \"B\" },\n  { \"id\": 3, \"score\": 78, \"category\": \"A\" },\n  { \"id\": 4, \"score\": 92, \"category\": \"C\" },\n  { \"id\": 5, \"score\": 88, \"category\": \"B\" }\n]\n</code></pre> <p>You can then copy this output to the Datasets view.</p>"},{"location":"honeysql/","title":"HoneySQL View","text":""},{"location":"honeysql/#why","title":"Why?","text":"<p>Because writing raw SQL strings is so 1999, and we prefer our SQL injection vulnerabilities to be at least slightly more difficult to achieve. Also, because you secretly love Lisp parentheses more than you love readable query languages.</p> <p>The HoneySQL View provides an interactive playground for HoneySQL v2, a library that allows you to represent SQL queries as Clojure data structures. This view enables real-time compilation of these data structures into standard SQL strings.</p>"},{"location":"honeysql/#features","title":"Features","text":"<ul> <li>Real-time Compilation: As you type your HoneySQL map, the corresponding   SQL string is generated instantly.</li> <li>Syntax Highlighting: Dedicated editors for Clojure input and SQL output.</li> <li>Formatted Output: The generated SQL is pretty-printed for readability.</li> </ul>"},{"location":"honeysql/#step-by-step-examples","title":"Step-by-Step Examples","text":""},{"location":"honeysql/#1-simple-select","title":"1. Simple Select","text":"<p>Let's write a query to select columns from our example dataset, assuming it exists in a database table named <code>dataset</code>.</p> <p>Steps:</p> <ol> <li>Navigate to the HoneySQL view.</li> <li> <p>In the left-hand editor, paste the following Clojure map:</p> <p><code>clojure {:select [:id :score :category :date]  :from   [:dataset]}</code></p> </li> <li> <p>Observe the output in the right-hand panel:</p> <p><code>sql SELECT id, score, category, date FROM dataset</code></p> </li> </ol>"},{"location":"honeysql/#2-filtering-and-sorting","title":"2. Filtering and Sorting","text":"<p>Now we will add conditions to filter by <code>category</code> and <code>score</code>, and sort by <code>date</code>, mirroring a typical data analysis task.</p> <p>Steps:</p> <ol> <li> <p>Update the input map to include <code>:where</code> and <code>:order-by</code> clauses:</p> <p><code>clojure {:select [:id :score]  :from   [:dataset]  :where  [:and           [:= :category \"a\"]           [:&gt; :score 10.0]]  :order-by [[:date :desc]]}</code></p> </li> <li> <p>The output will update to:</p> <p><code>sql SELECT id, score FROM dataset WHERE (category = 'a') AND (score &gt; 10.0) ORDER BY date DESC</code></p> </li> </ol>"},{"location":"honeysql/#3-aggregation-group-by","title":"3. Aggregation (Group By)","text":"<p>Let's calculate the average score per category.</p> <p>Steps:</p> <ol> <li> <p>Enter the following aggregation query:</p> <p><code>clojure {:select   [:category [[:avg :score] :avg_score]]  :from     [:dataset]  :group-by [:category]  :having   [:&gt; [[:avg :score]] 10.0]}</code></p> </li> <li> <p>The output will be:</p> <p><code>sql SELECT category, AVG(score) AS avg_score FROM dataset GROUP BY category HAVING AVG(score) &gt; 10.0</code></p> </li> </ol>"},{"location":"malli/","title":"Malli View","text":""},{"location":"malli/#why","title":"Why?","text":"<p>Because your data is messy, unorganized, and frankly, a little embarrassing. It's time to bring some law and order to that wild west of a map you call a \"data structure.\" Think of Malli as the strict librarian who shushes your data into compliance.</p> <p>The Malli View provides a comprehensive toolkit for working with Malli, a data-driven schema library for Clojure/Script. It enables you to infer schemas from data, generate synthetic data from schemas, validate values, and convert schemas to other formats like JSON Schema.</p>"},{"location":"malli/#features","title":"Features","text":"<ul> <li>Schema Inference: Automatically deduce a Malli schema from existing EDN   data samples.</li> <li>Data Generation: Produce random, property-based test data from a Malli   schema using <code>malli.generator</code>.</li> <li>Validation: Interactively check if a data value conforms to a given   schema.</li> <li>JSON Schema Conversion: Transform Malli schemas into standard JSON Schema   format for interoperability.</li> <li>Save Generated Data: Directly save generated datasets to the global   Datasets list.</li> </ul>"},{"location":"malli/#step-by-step-examples","title":"Step-by-Step Examples","text":""},{"location":"malli/#1-schema-inference","title":"1. Schema Inference","text":"<p>The inference tool is useful when you have raw data and want to formalize its structure.</p> <p>Steps:</p> <ol> <li>Navigate to the Malli view and select the Inference tab.</li> <li> <p>In the input editor, paste the following sample data (a subset of our     standard example):</p> <p><code>clojure [{:id 1 :score 12.5 :category \"a\" :date \"2023-01-01\"}  {:id 2 :score 10.2 :category \"b\" :date \"2023-01-02\"}]</code></p> </li> <li> <p>The view will automatically process the input.</p> </li> <li> <p>Observe the Inferred Schema in the right-hand panel. It should look     similar to:</p> <p><code>clojure [:vector  [:map   [:id :int]   [:score :double]   [:category :string]   [:date :string]]]</code></p> <p>(Note: The system may infer more specific types or enums depending on the data variety.)</p> </li> </ol>"},{"location":"malli/#2-data-generation","title":"2. Data Generation","text":"<p>You can use the inferred schema (or write your own) to generate more synthetic data.</p> <p>Steps:</p> <ol> <li>Switch to the Generation tab.</li> <li> <p>In the Schema editor, enter the schema for our example dataset:</p> <p><code>clojure [:vector  [:map   [:id :int]   [:score :double]   [:category [:enum \"a\" \"b\" \"c\"]]   [:date :string]]]</code></p> </li> <li> <p>Click the Generate button.</p> </li> <li>A list of randomly generated maps matching the schema will appear in the     output panel.</li> <li>(Optional) Click Save to Datasets to import this generated data into the     Datasets view for further analysis.</li> </ol>"},{"location":"malli/#3-validation","title":"3. Validation","text":"<p>Use the validation tool to debug why a specific value doesn't match a schema.</p> <p>Steps:</p> <ol> <li>Switch to the Validation tab.</li> <li> <p>In the Schema editor, enter:</p> <p><code>clojure [:map  [:id :int]  [:score :double]]</code></p> </li> <li> <p>In the Value editor, enter an invalid value (e.g., <code>score</code> as a string):</p> <p><code>clojure {:id 1  :score \"12.5\"}</code></p> </li> <li> <p>The output panel will display <code>Valid: false</code> and provide a detailed error     explanation, highlighting that <code>:score</code> expected a <code>:double</code> but got a     string.</p> </li> </ol>"},{"location":"malli/#4-json-schema-export","title":"4. JSON Schema Export","text":"<p>Steps:</p> <ol> <li>Switch to the JSON Schema tab.</li> <li>Paste the Malli schema from the previous examples.</li> <li>The equivalent JSON Schema object will be generated in the output panel,     ready for use in external systems.</li> </ol>"},{"location":"vega_lite/","title":"Vega-Lite View","text":""},{"location":"vega_lite/#why","title":"Why?","text":"<p>Because looking at raw numbers makes your eyes bleed, and you need a pretty chart to distract your manager from the fact that revenue is down. Or maybe you just like pretty colors. We don't judge. Much.</p> <p>The Vega-Lite View is a powerful environment for creating declarative statistical visualizations using Vega-Lite. It supports specifications written in both JSON (the native format) and EDN (Clojure data format), with automatic bidirectional conversion.</p>"},{"location":"vega_lite/#features","title":"Features","text":"<ul> <li>Dual-Format Editor: Write specifications in JSON or EDN. Switching formats   automatically converts your code.</li> <li>Live Preview: The chart renders in real-time as you edit the   specification.</li> <li>Dataset Integration: Access data stored in the global Datasets view   directly within your specification.</li> <li>Gist Support: Load specifications from GitHub Gists.</li> </ul>"},{"location":"vega_lite/#step-by-step-examples","title":"Step-by-Step Examples","text":""},{"location":"vega_lite/#1-basic-bar-chart-embedded-data","title":"1. Basic Bar Chart (Embedded Data)","text":"<p>We will visualize the \"score\" by \"category\" using our standard example data embedded directly in the specification.</p> <p>Steps:</p> <ol> <li>Navigate to the Vega-Lite view.</li> <li>Ensure the format toggle (top right of the editor) is set to EDN.</li> <li> <p>Paste the following code:</p> <p><code>clojure {:data {:values [{:id 1 :score 12.5 :category \"a\" :date \"2023-01-01\"}                  {:id 2 :score 10.2 :category \"b\" :date \"2023-01-02\"}                  {:id 3 :score 8.7  :category \"c\" :date \"2023-01-03\"}                  {:id 4 :score 15.0 :category \"a\" :date \"2023-01-04\"}                  {:id 5 :score 9.9  :category \"b\" :date \"2023-01-05\"}]}  :mark :bar  :encoding {:x {:field \"category\" :type \"nominal\" :axis {:title \"Category\"}}             :y {:field \"score\"    :type \"quantitative\" :axis {:title \"Score\"}}}}</code></p> </li> <li> <p>The bar chart will render in the right-hand panel.</p> </li> </ol>"},{"location":"vega_lite/#2-using-global-datasets","title":"2. Using Global Datasets","text":"<p>Instead of hardcoding data, you can reference a dataset you imported in the Datasets view.</p> <p>Prerequisites:</p> <ul> <li>You have a dataset named <code>example-data</code> loaded in the Datasets view (see   Datasets View).</li> </ul> <p>Steps:</p> <ol> <li> <p>In the Vega-Lite editor, replace the <code>:data</code> map with a reference to the     named dataset.</p> <ul> <li>Note: The specific syntax for referencing global named datasets depends   on the implementation. Typically, you use the <code>name</code> property in the data   object.</li> </ul> <p><code>clojure {:data {:name \"example-data\"}  :mark :point  :encoding {:x {:field \"date\"  :type \"temporal\"}             :y {:field \"score\" :type \"quantitative\"}             :color {:field \"category\" :type \"nominal\"}}}</code></p> </li> <li> <p>If the dataset is correctly loaded in the system, the scatter plot will     appear.</p> </li> </ol>"},{"location":"vega_lite/#3-converting-formats","title":"3. Converting Formats","text":"<p>Steps:</p> <ol> <li>With the EDN specification from Example 1 or 2 in the editor, click the     JSON toggle button.</li> <li> <p>The code will automatically convert to standard JSON:</p> <p><code>json {   \"data\": { \"name\": \"example-data\" },   \"mark\": \"point\",   \"encoding\": {     \"x\": { \"field\": \"date\", \"type\": \"temporal\" },     \"y\": { \"field\": \"score\", \"type\": \"quantitative\" },     \"color\": { \"field\": \"category\", \"type\": \"nominal\" }   } }</code></p> </li> <li> <p>You can edit in JSON and switch back to EDN at any time.</p> </li> </ol>"},{"location":"docs/CLI_DATA_STRUCTURES/","title":"CLI Data Structures","text":"<p>The <code>dstools data convert</code> command supports converting between different internal data structures. This is useful when you need to reshape data, for example, from a list of objects (row-maps) to an object of arrays (columnar).</p>"},{"location":"docs/CLI_DATA_STRUCTURES/#supported-structures","title":"Supported Structures","text":"<ul> <li><code>row-maps</code>: A sequence of maps. Example: <code>[{:a 1 :b 2} {:a 3 :b 4}]</code>.</li> <li><code>columnar</code>: A map of sequences. Example: <code>{:a [1 3] :b [2 4]}</code>.</li> <li><code>rows</code>: A sequence of sequences, where the first element is the header.   Example: <code>[[:a :b] [1 2] [3 4]]</code>.</li> </ul>"},{"location":"docs/CLI_DATA_STRUCTURES/#structure-guessing","title":"Structure Guessing","text":"<p>If you do not specify an input structure using <code>--input-struct</code> (or <code>-S</code>), the tool attempts to guess it based on the parsed data:</p> <ol> <li>Row Maps: If the data is a sequence and the first element is a map.</li> <li>Columnar: If the data is a map.</li> <li>Rows: If the data is a sequence and the first element is also a     sequence.</li> </ol>"},{"location":"docs/CLI_DATA_STRUCTURES/#explicit-conversion","title":"Explicit Conversion","text":"<p>You can enforce specific input and output structures using the options:</p> <ul> <li><code>-S, --input-struct &lt;struct&gt;</code>: Declare the structure of the input data.</li> <li><code>-s, --output-struct &lt;struct&gt;</code>: Request the structure of the output data.</li> </ul> <p>If <code>--output-struct</code> is provided but <code>--input-struct</code> is not, the tool will guess the input structure and convert it to the requested output structure. If neither is provided, the structure remains unchanged (except for CSV output, which always requires/produces a row-based format internally before writing).</p>"},{"location":"docs/CLI_DATA_STRUCTURES/#examples","title":"Examples","text":""},{"location":"docs/CLI_DATA_STRUCTURES/#convert-row-maps-to-columnar","title":"Convert Row Maps to Columnar","text":"<p>Given <code>data.json</code>:</p> <pre><code>[\n  { \"a\": 1, \"b\": 2 },\n  { \"a\": 3, \"b\": 4 }\n]\n</code></pre> <p>Command:</p> <pre><code>bb -m dstools data convert -f json -t json -s columnar -i data.json\n</code></pre> <p>Output:</p> <pre><code>{ \"a\": [1, 3], \"b\": [2, 4] }\n</code></pre>"},{"location":"docs/CLI_DATA_STRUCTURES/#convert-columnar-to-row-maps","title":"Convert Columnar to Row Maps","text":"<p>Given <code>cols.json</code>:</p> <pre><code>{ \"a\": [1, 3], \"b\": [2, 4] }\n</code></pre> <p>Command:</p> <pre><code>bb -m dstools data convert -f json -t json -s row-maps -i cols.json\n</code></pre> <p>Output:</p> <pre><code>[\n  { \"a\": 1, \"b\": 2 },\n  { \"a\": 3, \"b\": 4 }\n]\n</code></pre>"},{"location":"docs/DESIGN_WORKSPACES/","title":"Design Doc: Workspaces and User Input Persistency","text":""},{"location":"docs/DESIGN_WORKSPACES/#overview","title":"Overview","text":"<p>This document outlines the design for adding \"Workspaces\" and \"User Input Persistency\" to the <code>bb-web-ds-tools</code> application. The goal is to allow users to switch between different environments (workspaces) and persist their inputs (datasets, schemas, code, configs) across sessions.</p>"},{"location":"docs/DESIGN_WORKSPACES/#architecture","title":"Architecture","text":"<p>We will use a layered architecture:</p> <ol> <li>UI Layer: React/Reagent components for managing workspaces (create,     switch, delete) and displaying/editing inputs.</li> <li>State Management Layer: <code>re-frame</code> for UI state, backed by     DataScript for the domain model (workspaces, inputs).</li> <li>Persistence Layer: SQL.wasm (SQLite) to persist the DataScript     database (or a serialized version of it) to the browser's storage (e.g.,     IndexedDB or OPFS via SQL.js).</li> </ol>"},{"location":"docs/DESIGN_WORKSPACES/#libraries","title":"Libraries","text":"<ul> <li>DataScript: An immutable, in-memory database and Datalog query engine for   ClojureScript. It will serve as the \"source of truth\" for the workspace   metadata and content while the app is running.</li> <li>SQL.wasm (<code>sql.js</code>): A port of SQLite to WebAssembly. It will be used to   persist the workspace data to a SQLite database file, which can be saved to   the browser's storage or exported/imported by the user.</li> </ul>"},{"location":"docs/DESIGN_WORKSPACES/#data-model","title":"Data Model","text":"<p>The DataScript schema will define the following entities:</p>"},{"location":"docs/DESIGN_WORKSPACES/#workspace","title":"Workspace","text":"<ul> <li><code>:workspace/id</code> (uuid, unique identity)</li> <li><code>:workspace/name</code> (string)</li> <li><code>:workspace/created-at</code> (inst)</li> <li><code>:workspace/updated-at</code> (inst)</li> </ul>"},{"location":"docs/DESIGN_WORKSPACES/#input","title":"Input","text":"<ul> <li><code>:input/id</code> (uuid, unique identity)</li> <li><code>:input/workspace</code> (ref to <code>:workspace/id</code>)</li> <li><code>:input/type</code> (keyword/enum: <code>:dataset</code>, <code>:schema</code>, <code>:code</code>, <code>:config</code>)</li> <li><code>:input/name</code> (string, e.g., filename or identifier)</li> <li><code>:input/content</code> (string or blob - the actual user input)</li> <li><code>:input/metadata</code> (map - optional extra info like language, format)</li> <li><code>:input/updated-at</code> (inst)</li> </ul>"},{"location":"docs/DESIGN_WORKSPACES/#persistence-strategy","title":"Persistence Strategy","text":"<ol> <li> <p>On Load:</p> <ul> <li>Initialize SQL.wasm.</li> <li>Load the SQLite database from IndexedDB (or similar local storage).</li> <li>Query the SQLite DB for all workspaces and inputs.</li> <li>Hydrate the DataScript DB with this data.</li> <li>Populate <code>re-frame</code> app-db with the active workspace.</li> </ul> </li> <li> <p>On Change:</p> <ul> <li>When a user modifies an input or switches workspaces, update the   DataScript DB transactionally.</li> <li>(Optionally) Debounce and sync the changes to the SQL.wasm DB.</li> <li>Persist the SQL.wasm DB file to storage.</li> </ul> </li> </ol>"},{"location":"docs/DESIGN_WORKSPACES/#ui-ux","title":"UI / UX","text":"<ul> <li>Workspace Selector: A dropdown or modal in the top navigation/sidebar to   switch workspaces.</li> <li>Input Management: The existing views (Malli, HoneySQL, Editor, etc.) will   need to be updated to bind their state to the current workspace's relevant   input.</li> <li>For example, the \"Malli\" view currently might use local state or a global     atom. It should now subscribe to the \"active schema input\" of the current     workspace.</li> </ul>"},{"location":"docs/DESIGN_WORKSPACES/#implementation-details","title":"Implementation Details","text":"<ul> <li>Namespace: <code>bb-web-ds-tools.workspaces</code></li> <li>Components: <code>workspace-selector</code>, <code>input-manager</code></li> <li>Events: <code>::add-workspace</code>, <code>::switch-workspace</code>, <code>::save-input</code></li> </ul>"},{"location":"docs/DESIGN_WORKSPACES/#semantic-usage-mapping","title":"Semantic Usage Mapping","text":"<ul> <li>Datasets: Mapped to <code>:input/type :dataset</code>. Used in Data Grid, Vega-Lite,   etc.</li> <li>Schema: Mapped to <code>:input/type :schema</code>. Used in Malli views.</li> <li>Code: Mapped to <code>:input/type :code</code>. Used in Editors (Clojure, Python, R).</li> <li>Configs: Mapped to <code>:input/type :config</code>. Used in Settings, Vega-Lite   config.</li> </ul>"},{"location":"docs/DESIGN_WORKSPACES/#testing","title":"Testing","text":"<ul> <li>Unit tests for DataScript schema and transactions.</li> <li>Integration tests for the persistence layer (mocking SQL.wasm if needed).</li> <li>E2E tests for workspace switching and data persistence.</li> </ul>"},{"location":"docs/POST_MORTEM_APP_DISPLAY/","title":"Post Mortem: App Not Displaying","text":""},{"location":"docs/POST_MORTEM_APP_DISPLAY/#date","title":"Date","text":"<p>2025-12-01</p>"},{"location":"docs/POST_MORTEM_APP_DISPLAY/#issue","title":"Issue","text":"<p>The application failed to display (blank screen) after building and running the release artifact (<code>docs/</code>). E2E tests failed with timeouts waiting for the landing page content.</p>"},{"location":"docs/POST_MORTEM_APP_DISPLAY/#root-cause","title":"Root Cause","text":"<p>The <code>shadow-cljs</code> build configuration in <code>shadow-cljs.edn</code> defines multiple modules (<code>:main</code>, <code>:shared</code>, <code>:sci-worker</code>, <code>:pyodide-worker</code>). When using code splitting with modules, the <code>:main</code> module depends on the <code>:shared</code> module. However, <code>docs/index.html</code> was only loading <code>main.js</code> and <code>libs.js</code>, but failing to load <code>shared.js</code>.</p> <p>This caused a runtime error (<code>SHADOW_ENV is not defined</code> or similar dependency errors) because the shared runtime code was not loaded before <code>main.js</code> execution.</p> <p>Additionally, the <code>Gemma</code> view was commented out in <code>src/bb_web_ds_tools/core.cljs</code>, causing verification failures for that specific route. The <code>Repl</code> view was missing the \"Add REPL\" button expected by the verification script.</p>"},{"location":"docs/POST_MORTEM_APP_DISPLAY/#resolution","title":"Resolution","text":"<ol> <li>Updated <code>docs/index.html</code>: Added     <code>&lt;script src=\"js/compiled/shared.js\" type=\"text/javascript\"&gt;&lt;/script&gt;</code>     before <code>main.js</code> to ensure shared code is loaded first.</li> <li>Restored <code>Gemma</code> View: Uncommented the <code>gemma</code> require and <code>defmethod</code>     in <code>src/bb_web_ds_tools/core.cljs</code>.</li> <li>Updated <code>Repl</code> View: Added the \"Add REPL\" button to     <code>src/bb_web_ds_tools/views/repl.cljs</code> to match E2E test expectations and     restore functionality.</li> </ol>"},{"location":"docs/POST_MORTEM_APP_DISPLAY/#verification","title":"Verification","text":"<p>Ran <code>python3 test/e2e/verify_release.py</code> after rebuilding the app. All pages, including Landing, Gemma, and Repl, verified successfully.</p>"},{"location":"docs/VISUAL_BUG_REPORT/","title":"Visual Bug Report","text":""},{"location":"docs/VISUAL_BUG_REPORT/#1-hardcoded-hex-codes","title":"1. Hardcoded Hex Codes","text":"<p>The following elements use undefined hex codes instead of semantic theme tokens, violating the design system consistency.</p> <ul> <li>File: <code>src/bb_web_ds_tools/views/datasets.cljs</code></li> <li>Element: Dataset Title Input (Focus State)</li> <li>Issue: Uses <code>focus:border-[#f0dfaf]</code>.</li> <li>Fix: Create/Use a theme token for the accent border color (e.g.,     <code>t/border-focus-accent</code>).</li> <li>Element: Delete Dataset Button</li> <li>Issue: Uses <code>text-[#3f3f3f]</code>.</li> <li>Fix: Use <code>t/text-button-primary</code> (which maps to the same color but is     semantically defined).</li> </ul>"},{"location":"docs/VISUAL_BUG_REPORT/#2-grid-spacing-deviations","title":"2. Grid &amp; Spacing Deviations","text":"<p>The \"Dense Data Grid\" component deviates from the \"Engineering-Grade\" spec.</p> <ul> <li>File: <code>src/bb_web_ds_tools/components/common.cljs</code></li> <li>Component: <code>th</code>, <code>td</code></li> <li>Issue: Uses <code>px-6 py-3</code> (th) and <code>px-6 py-4</code> (td).</li> <li>Spec Violation: The spec requires <code>px-3</code> padding and 32px row height.     The current padding results in significantly taller rows (&gt;50px) and wider     columns.</li> <li>Fix: Update padding to <code>px-3</code> and adjust vertical padding (e.g.,     <code>py-1.5</code> or <code>py-2</code>) to match the 32px height constraint.</li> </ul>"},{"location":"docs/VISUAL_BUG_REPORT/#3-optical-alignment-failures","title":"3. Optical Alignment Failures","text":"<p>Instances where elements are mathematically aligned but visually unbalanced.</p> <ol> <li> <p>Table Header Sort Indicators:</p> <ul> <li>The sort arrows (<code>\u25b2</code>/<code>\u25bc</code>) are appended as text characters within the <code>th</code>   element. Due to font metrics, these glyphs often do not align with the   cap-height of the header text, causing them to float or sink visually.</li> </ul> </li> <li> <p>Dataset View Title vs. Row Count:</p> <ul> <li>The dataset title is <code>text-xl</code> (20px/28px) and the row count is <code>text-sm</code>   (14px/20px). While aligned via <code>items-center</code> (flexbox), the significant   difference in baseline and cap-height makes the smaller text feel   \"floating\" relative to the title's baseline.</li> </ul> </li> <li> <p>Importer View Buttons vs. Labels:</p> <ul> <li>The <code>c/button-xs</code> components are mixed with text labels like \"Load   Example:\". The button padding (<code>py-1</code>) combined with the border and text   size creates a container height that may not align its text baseline with   the adjacent static label text, creating a \"stair-step\" effect.</li> </ul> </li> </ol>"},{"location":"docs/client_side_filesystem/","title":"Client-Side File System &amp; Persistence Design","text":""},{"location":"docs/client_side_filesystem/#1-overview","title":"1. Overview","text":"<p>This document outlines the design for implementing a persistent, client-side file system for <code>bb-web-ds-tools</code>. The goal is to allow users to create \"workspaces\" containing files (code, datasets, configurations) that persist across browser sessions and reloads, without relying on a backend server or manual file export/import.</p> <p>The solution leverages:</p> <ul> <li>SQLite WASM: For robust, SQL-based storage in the browser.</li> <li>Origin Private File System (OPFS): For performant, persistent backing of   the SQLite database.</li> <li>DataScript: For reactive, in-memory state management within the UI.</li> <li>Web Workers: To offload database I/O from the main thread.</li> </ul>"},{"location":"docs/client_side_filesystem/#2-architecture","title":"2. Architecture","text":""},{"location":"docs/client_side_filesystem/#21-storage-layer-sqlite-opfs","title":"2.1 Storage Layer: SQLite + OPFS","text":"<p>The core storage mechanism will be an SQLite database running in the browser.</p> <ul> <li>Persistence: Instead of the current <code>:memory:</code> database, we will use the   <code>opfs</code> VFS (Virtual File System) provided by the SQLite WASM build. This   stores the database file in the browser's Origin Private File System, which is   persistent and optimized for random access.</li> <li>File Format: Standard SQLite <code>.db</code> file.</li> <li>Tables:</li> <li><code>workspaces</code>: Stores workspace metadata (id, name, dates).</li> <li><code>files</code>: Stores the actual user content (code, datasets). This replaces the     <code>inputs</code> concept to be more filesystem-like.</li> </ul> <pre><code>CREATE TABLE workspaces (\n    id TEXT PRIMARY KEY,\n    name TEXT NOT NULL,\n    created_at INTEGER,\n    updated_at INTEGER\n);\n\nCREATE TABLE files (\n    id TEXT PRIMARY KEY,\n    workspace_id TEXT NOT NULL,\n    parent_id TEXT, -- For folder structure (optional future proofing)\n    name TEXT NOT NULL,\n    type TEXT NOT NULL, -- 'dir', 'file'\n    mime_type TEXT,     -- 'text/x-clojure', 'application/json', etc.\n    content BLOB,       -- Text or binary content\n    metadata JSON,      -- Extra app-specific metadata\n    created_at INTEGER,\n    updated_at INTEGER,\n    FOREIGN KEY(workspace_id) REFERENCES workspaces(id) ON DELETE CASCADE\n);\n</code></pre>"},{"location":"docs/client_side_filesystem/#22-worker-based-io-sequential-execution","title":"2.2 Worker-Based I/O &amp; Sequential Execution","text":"<p>The OPFS VFS in SQLite WASM is most performant when used from a dedicated Web Worker. This prevents large read/write operations from freezing the UI.</p> <ul> <li>Persistence Worker: A new worker   (<code>src/bb_web_ds_tools/workers/persistence.cljs</code>) will be created.</li> <li>Responsibilities:</li> <li>Initialize the SQLite DB with OPFS backend.</li> <li>Handle messages: <code>:load-all</code>, <code>:save-file</code>, <code>:delete-file</code>,     <code>:create-workspace</code>, etc.</li> <li>Post updates back to the main thread.</li> </ul> <p>Sequential Execution Strategy: While the main thread communicates with the worker asynchronously (non-blocking), operations within the worker are handled synchronously and sequentially.</p> <ol> <li>Message Queue: The worker's <code>onmessage</code> handler receives events from the     main thread.</li> <li>Sequential Processing: JavaScript's single-threaded event loop ensures     that each message is processed to completion before the next one starts.</li> <li>Synchronous SQLite: The worker will use the synchronous API of SQLite     WASM (standard for the worker build). This guarantees that a write operation     (e.g., <code>INSERT INTO files...</code>) is fully committed to OPFS before the next     operation begins.</li> <li>Consistency: This design ensures data consistency (ACID) without race     conditions within the database itself, even if the user triggers multiple     rapid saves.</li> </ol>"},{"location":"docs/client_side_filesystem/#23-state-management-datascript-sqlite-sync","title":"2.3 State Management: DataScript &lt;-&gt; SQLite Sync","text":"<p>The application currently uses DataScript for fast, queryable, reactive state in the UI. We will maintain this but treat it as a \"cache\" or \"working copy\" of the persistent SQLite DB.</p> <ul> <li>Load Strategy: On app startup, the Persistence Worker fetches all   workspace/file metadata (not content) and sends it to the main thread to   populate DataScript. Content is lazy-loaded on demand or pre-loaded if small.</li> <li>Write Strategy:</li> <li>User edits a file -&gt; Update DataScript (immediate UI feedback).</li> <li>Debounced effect -&gt; Send <code>:save-file</code> message to Persistence Worker.</li> <li>Worker writes to SQLite -&gt; Confirms save (update \"saved\" status in UI).</li> </ul>"},{"location":"docs/client_side_filesystem/#3-implementation-steps","title":"3. Implementation Steps","text":""},{"location":"docs/client_side_filesystem/#phase-1-persistence-worker-opfs","title":"Phase 1: Persistence Worker &amp; OPFS","text":"<ol> <li>Worker Setup: Create a generic worker infrastructure for persistence     (similar to <code>sci</code> or <code>pyodide</code> workers).</li> <li>SQLite OPFS Init: Modify the SQLite initialization logic to use the     <code>opfs</code> pool if available (requires proper HTTP headers     <code>Cross-Origin-Opener-Policy: same-origin</code> and     <code>Cross-Origin-Embedder-Policy: require-corp</code> on the server serving the app,     or fallback to IndexedDB/LocalStorage if strictly necessary, though OPFS is     preferred).<ul> <li>Note: GitHub Pages supports these headers via a hack or by using the   Service Worker fallback provided by SQLite WASM for environments without   COOP/COEP.</li> </ul> </li> <li>CRUD Operations: Implement handlers for creating workspaces and files in     the worker.</li> </ol>"},{"location":"docs/client_side_filesystem/#phase-2-ui-integration","title":"Phase 2: UI Integration","text":"<ol> <li>Service Layer: Create <code>bb-web-ds-tools.services.persistence</code> to handle     <code>postMessage</code> communication with the worker.</li> <li>Re-frame Effects: Register effects (e.g., <code>::persistence/save</code>) that     delegate to the service layer.</li> <li>Boot Sequence: Update <code>core.cljs/init</code> to wait for the persistence     worker to signal \"ready\" and load initial data before rendering the main UI.</li> </ol>"},{"location":"docs/client_side_filesystem/#phase-3-virtual-file-system-vfs-ui","title":"Phase 3: Virtual File System (VFS) UI","text":"<ol> <li>File Explorer: Create a UI component     (<code>bb-web-ds-tools.views.workspaces.explorer</code>) that visualizes the     <code>workspaces</code> and <code>files</code> tables as a tree.</li> <li>File Operations: Context menus for Rename, Delete, Move (changing     <code>parent_id</code> or <code>workspace_id</code>).</li> <li>Editor Integration: When a file is selected, load its content into the     appropriate editor (Monaco, generic text, or visual editor like Malli/Vega).</li> </ol>"},{"location":"docs/client_side_filesystem/#4-data-migration-schema","title":"4. Data Migration &amp; Schema","text":"<p>The current <code>inputs</code> schema in DataScript will be mapped to the <code>files</code> table.</p> <ul> <li><code>input/type</code> -&gt; <code>files.mime_type</code> (mapped)</li> <li><code>input/name</code> -&gt; <code>files.name</code></li> <li><code>input/content</code> -&gt; <code>files.content</code></li> </ul> <p>Since there is no existing persistent data (only manual exports), no complex migration strategy for existing users is needed yet.</p>"},{"location":"docs/client_side_filesystem/#5-challenges-mitigations","title":"5. Challenges &amp; Mitigations","text":"<ul> <li>Concurrency: If two tabs open the same workspace, SQLite OPFS handles   locking, but the UI might get out of sync.</li> <li>Mitigation: Use <code>BroadcastChannel</code> in the worker to notify other tabs of     changes, triggering a re-fetch in DataScript.</li> <li>Binary Data: Datasets might be binary (Parquet, Arrow).</li> <li>Mitigation: Use <code>BLOB</code> storage in SQLite and transfer <code>ArrayBuffers</code>     between worker and main thread.</li> <li>COOP/COEP Headers: Required for high-performance OPFS.</li> <li>Mitigation: Ensure the deployment environment (GitHub Pages) is     compatible, or use the \"promiser\" API of SQLite WASM which proxies to a     worker that can use a slightly slower but more compatible backend if needed.</li> </ul>"},{"location":"docs/dataset_sync_design/","title":"Dataset Synchronization Design","text":"<p>This document describes the design of the dataset synchronization mechanism between the main thread and web workers (specifically the SCI worker).</p>"},{"location":"docs/dataset_sync_design/#overview","title":"Overview","text":"<p>The application maintains a global state of datasets in the main thread (re-frame <code>app-db</code>). Workers need to access and potentially modify these datasets. The synchronization mechanism ensures that changes in the main thread are propagated to the worker, and changes in the worker are propagated back to the main thread.</p>"},{"location":"docs/dataset_sync_design/#mechanism","title":"Mechanism","text":""},{"location":"docs/dataset_sync_design/#main-thread-worker","title":"Main Thread -&gt; Worker","text":"<p>The <code>bb-web-ds-tools.runtime.sci</code> namespace subscribes to <code>::datasets/items</code>. A watcher on this subscription sends an <code>update-datasets</code> message to the worker whenever the datasets change.</p> <pre><code>;; bb-web-ds-tools.runtime.sci\n(worker/post-message @sci-worker {:type \"update-datasets\" :datasets new-val})\n</code></pre>"},{"location":"docs/dataset_sync_design/#worker-main-thread","title":"Worker -&gt; Main Thread","text":"<p>The worker (<code>bb-web-ds-tools.workers.sci</code>) maintains a <code>datasets-atom</code> which is exposed to the SCI context as <code>user/datasets</code>.</p> <ol> <li>Watch: A watcher is added to <code>datasets-atom</code> to detect changes.</li> <li>Diff: When the atom changes, the watcher calculates the difference (added or updated keys) between the old and new state.</li> <li>Patch: A <code>:patch-datasets</code> message containing only the changed datasets is sent to the main thread.</li> <li>Suppression: To prevent infinite loops, updates originating from the main thread (via <code>update-datasets</code> message) are applied within a dynamic binding <code>*suppress-sync*</code> set to true, which prevents the watcher from triggering a feedback loop.</li> </ol> <pre><code>;; bb-web-ds-tools.workers.sci\n(add-watch datasets-atom :sync-main\n  (fn [_ _ old-state new-state]\n    (when-not *suppress-sync*\n      (let [patch (calculate-diff old-state new-state)]\n        (post-msg {:type :patch-datasets :patch patch})))))\n</code></pre>"},{"location":"docs/dataset_sync_design/#main-thread-handling","title":"Main Thread Handling","text":"<p>The main thread receives the <code>:patch-datasets</code> message and dispatches <code>::datasets/patch-datasets</code> event, which merges the patch into the app-db.</p>"},{"location":"docs/dataset_sync_design/#sql-wasm-integration-extendability","title":"SQL WASM Integration (Extendability)","text":"<p>This mechanism is extendable to support workflows where a worker uses SQL WASM to persist data and notify the main thread.</p>"},{"location":"docs/dataset_sync_design/#workflow","title":"Workflow","text":"<ol> <li>Worker Write: The worker (e.g., executing user code via SCI) writes data to a local SQL WASM database (backed by OPFS or memory).</li> <li>Dataset Update: After writing, the worker updates the <code>user/datasets</code> atom. This can be done by:<ul> <li>Reading the data back from SQL and putting it into the dataset map.</li> <li>Or, if the data is large, creating a dataset entry that contains metadata or a reference (e.g., file path) pointing to the SQL resource.</li> </ul> </li> <li>Sync: The <code>datasets-atom</code> watcher detects this change and syncs the new dataset entry (containing the data or reference) to the main thread.</li> <li>Main Thread Action: The main thread receives the update. If the dataset contains a reference (e.g., <code>{:type :sql-ref :path \"/mydb.sqlite3\"}</code>), the main thread can then act on it (e.g., if it has access to the same OPFS, it can read the file, or it can display a UI element indicating the data is in SQL).</li> </ol>"},{"location":"docs/dataset_sync_design/#example-worker-code","title":"Example: Worker Code","text":"<pre><code>;; In the SCI REPL (Worker)\n(require '[bb-web-ds-tools.workers.persistence :as p]) ;; Assuming persistence is exposed\n\n;; 1. Write to SQL\n(p/execute! \"INSERT INTO results (val) VALUES (42)\")\n\n;; 2. Read back\n(let [results (p/query \"SELECT * FROM results\")]\n  ;; 3. Update datasets\n  (swap! user/datasets assoc :sql-results {:name \"SQL Results\" :data results}))\n\n;; The watcher automatically syncs :sql-results to main thread.\n</code></pre> <p>To fully enable this, <code>bb-web-ds-tools.workers.sci</code> would need to expose the SQL functions (from <code>persistence.cljs</code>) in the SCI context. Currently, <code>persistence.cljs</code> is a separate module, but the synchronization pattern described above handles the communication aspect regardless of how the data is generated.</p>"},{"location":"docs/fsm_logic/","title":"Finite State Machine Logic &amp; Event Mapping","text":"<p>This document describes the logic behind the Finite State Machine (FSM) schema and the event mapping for the <code>bb-web-ds-tools</code> application.</p>"},{"location":"docs/fsm_logic/#event-mapping","title":"Event Mapping","text":"<p>The <code>bb-web-ds-tools.events.journeys</code> namespace contains a <code>view-events</code> map that catalogs all re-frame events triggered by different views in the application.</p> <p>This mapping serves as a reference for:</p> <ul> <li>Understanding which events belong to which domain (View).</li> <li>Designing user journeys and test scenarios.</li> <li>auditing the application's event surface.</li> </ul>"},{"location":"docs/fsm_logic/#structure","title":"Structure","text":"<p>The map is keyed by the view name (as a keyword, e.g., <code>:malli</code>, <code>:datasets</code>) and the value is a vector of event keywords (e.g., <code>[:malli/initialize :malli/infer-schema]</code>).</p>"},{"location":"docs/fsm_logic/#finite-state-machine-fsm-schema","title":"Finite State Machine (FSM) Schema","text":"<p>A recursive Malli schema <code>fsm-schema</code> is defined to model User Journeys as a Hierarchical Finite State Machine. This allows representing complex interactions where a state can itself contain a sub-machine (e.g., a \"Form\" state containing \"Input\", \"Validation\", \"Submission\" sub-states).</p>"},{"location":"docs/fsm_logic/#schema-definition","title":"Schema Definition","text":"<p>The schema is registered with the following structure:</p> <pre><code>[:schema\n {:registry\n  {::state [:map\n            [:name keyword?]\n            [:type [:enum :atomic :compound :final]]\n            [:initial {:optional true} keyword?] ;; Required for :compound states\n            [:states {:optional true} [:map-of keyword? [:ref ::state]]] ;; Nested states\n            [:on {:optional true}\n             [:map-of keyword? ;; Event triggering the transition\n              [:map\n               [:target keyword?] ;; The name of the next state\n               [:action {:optional true} keyword?] ;; Optional side-effect identifier\n               [:guard {:optional true} fn?]]]]]} ;; Optional guard predicate\n  }\n [:ref ::state]]\n</code></pre>"},{"location":"docs/fsm_logic/#components","title":"Components","text":"<ol> <li> <p>State Types (<code>:type</code>):</p> <ul> <li><code>:atomic</code>: A simple state with no internal structure.</li> <li><code>:compound</code>: A state that contains sub-states (requires <code>:initial</code> and   <code>:states</code>).</li> <li><code>:final</code>: A terminal state indicating the end of a journey.</li> </ul> </li> <li> <p>Transitions (<code>:on</code>):</p> <ul> <li>A map where keys are event names (from <code>view-events</code>) and values describe   the transition.</li> <li><code>[:target keyword?]</code>: The destination state.</li> <li><code>[:action keyword?]</code>: An optional action to perform during transition.</li> <li><code>[:guard fn?]</code>: A predicate function that must return true for the   transition to occur.</li> </ul> </li> </ol>"},{"location":"docs/fsm_logic/#usage-example","title":"Usage Example","text":"<pre><code>(def my-journey\n  {:name :dataset-creation\n   :type :compound\n   :initial :idle\n   :states {:idle {:name :idle\n                   :type :atomic\n                   :on {:user/clicked-create {:target :creating}}}\n            :creating {:name :creating\n                       :type :compound\n                       :initial :input\n                       :states {:input {:name :input\n                                        :type :atomic\n                                        :on {:user/entered-data {:target :validating}}}\n                                :validating {:name :validating\n                                             :type :atomic\n                                             :on {:validation/success {:target :saving}\n                                                  :validation/failure {:target :input}}}}\n                       :on {:user/cancelled {:target :idle}}}}})\n</code></pre> <p>This structure allows for modeling the application's behavior in a structured, verifiable way.</p>"},{"location":"docs/maintenance/","title":"Code Maintenance Guide","text":"<p>This document outlines the best practices for maintaining the <code>bb-web-ds-tools</code> codebase.</p>"},{"location":"docs/maintenance/#1-project-structure-namespaces","title":"1. Project Structure &amp; Namespaces","text":"<p>The project is organized into several key namespaces under <code>src/bb_web_ds_tools/</code>:</p> <ul> <li><code>core</code>: The application entry point and main router logic.</li> <li><code>events</code>: Contains re-frame event handlers. <code>journeys.cljc</code> is central for   Event Mapping and FSM logic.</li> <li><code>views</code>: Reagent components representing different pages or features   (e.g., <code>malli</code>, <code>datasets</code>, <code>r-repl</code>).</li> <li><code>components</code>: Reusable UI components (buttons, inputs, layout).</li> <li><code>utils</code>: Pure functions for data processing, datasets, and helpers.</li> <li><code>runtime</code>: Manages execution environments like SCI and Pyodide, often   using Web Workers.</li> <li><code>theme</code>: Central definition for design tokens and colors (Zenburn   palette).</li> </ul>"},{"location":"docs/maintenance/#2-finite-state-machine-fsm","title":"2. Finite State Machine (FSM)","text":"<p>The application uses a Finite State Machine concept for complex user journeys.</p> <ul> <li>Logic Definition: The logic and schema are documented in   <code>docs/fsm_logic.md</code>.</li> <li>Implementation: The code resides in   <code>src/bb_web_ds_tools/events/journeys.cljc</code>.</li> <li>Updating the FSM:</li> <li>If you add new views or events, update the <code>view-events</code> map in       <code>journeys.cljc</code>.</li> <li>If the FSM structure requires changes, update <code>fsm-schema</code> in       <code>journeys.cljc</code> and ensure it remains consistent with <code>docs/fsm_logic.md</code>.</li> </ul>"},{"location":"docs/maintenance/#3-testing-strategy","title":"3. Testing Strategy","text":"<p>We employ a multi-layered testing strategy.</p>"},{"location":"docs/maintenance/#unit-integration-tests-clojurescript","title":"Unit &amp; Integration Tests (ClojureScript)","text":"<ul> <li>Run: <code>npm test</code> (Runs Karma via Shadow-CLJS).</li> <li>Compile Check: <code>npx shadow-cljs compile test</code>.</li> <li>Note: Ensure <code>shadow-cljs.edn</code> is configured correctly for the test build.</li> </ul>"},{"location":"docs/maintenance/#end-to-end-e2e-tests","title":"End-to-End (E2E) Tests","text":"<ul> <li>Run: <code>python3 test/e2e/verify_release.py</code>.</li> <li>Prerequisite: A production build must exist   (<code>npx shadow-cljs release app &amp;&amp; npx webpack --config-name app</code>).</li> <li>Scope: Verifies that the compiled application loads and critical paths   (navigation, runtime initialization) work in a real browser environment.</li> </ul>"},{"location":"docs/maintenance/#4-documenting-failures","title":"4. Documenting Failures","text":"<p>When tests fail or bugs are discovered, documentation is key to preventing recurrence.</p> <ul> <li>Create a Post Mortem: Specific complex failures (like build issues or   blank screens) should be documented in a file like   <code>docs/POST_MORTEM_&lt;ISSUE_NAME&gt;.md</code>.</li> <li>Content:</li> <li>Date: When it happened.</li> <li>Issue: What failed (symptoms).</li> <li>Root Cause: Why it failed (technical explanation).</li> <li>Resolution: How it was fixed.</li> <li>Verification: How we ensured the fix works.</li> </ul> <p>Refer to <code>docs/POST_MORTEM_APP_DISPLAY.md</code> for an example.</p>"},{"location":"docs/persistence_design/","title":"Persistence Design","text":"<p>This document outlines the design decisions and trade-offs for the client-side persistence layer of <code>bb-web-ds-tools</code>.</p>"},{"location":"docs/persistence_design/#overview","title":"Overview","text":"<p>The application uses SQLite WASM to persist user data (specifically Datasets and Workspaces) in the browser. This allows users to retain their work across page reloads and browser sessions.</p>"},{"location":"docs/persistence_design/#architecture","title":"Architecture","text":""},{"location":"docs/persistence_design/#storage-engine","title":"Storage Engine","text":"<ul> <li>SQLite WASM: We use the official <code>@sqlite.org/sqlite-wasm</code> build.</li> <li>Backend: We primarily target the <code>JsStorageDb</code> backend (if available),   which persists data to <code>localStorage</code> or <code>sessionStorage</code>. This provides a   simple, synchronous-like API that works well for the scale of data typical in   this application (up to ~5-10MB).</li> <li>Fallback: If <code>JsStorageDb</code> is unavailable, we fall back to an in-memory   database (<code>:memory:</code>), where persistence is limited to the current session   (effectively non-persistent across reloads, but functional for the session).</li> </ul>"},{"location":"docs/persistence_design/#serialization","title":"Serialization","text":"<ul> <li>Transit: Data stored in the SQLite database is serialized using   <code>cognitect.transit</code> (JSON encoding). This handles Clojure-specific types   (keywords, sets, dates) robustly compared to raw JSON.</li> <li>Structure:</li> <li>Datasets: Stored in a <code>datasets</code> table as     <code>(id TEXT, name TEXT, content TEXT, created_at INTEGER)</code>. The <code>content</code>     column holds the Transit-encoded string of the dataset map.</li> <li>Workspaces: Stored in <code>workspaces</code> and <code>inputs</code> tables, mirroring the     DataScript schema.</li> </ul>"},{"location":"docs/persistence_design/#state-management","title":"State Management","text":"<ul> <li>Re-frame Integration:</li> <li>Pure Events: Events like <code>::save-datasets</code> are pure; they update the     <code>app-db</code> status (e.g., <code>:persistence/status :saving</code>) and return a     side-effect description.</li> <li>Effects Handler: The actual database operations are performed in     <code>reg-fx</code> handlers defined in <code>bb-web-ds-tools.workspaces.persistence-fx</code>.     This isolates side effects.</li> <li>Asynchrony: While <code>sqlite-wasm</code> operations in the main thread are     synchronous, the <code>reg-fx</code> pattern decouples the UI update cycle from the DB     operation.</li> </ul>"},{"location":"docs/persistence_design/#performance-threading","title":"Performance &amp; Threading","text":""},{"location":"docs/persistence_design/#current-implementation-main-thread","title":"Current Implementation: Main Thread","text":"<p>Currently, the SQLite operations run on the main UI thread.</p> <p>Reasoning:</p> <ol> <li>Complexity: Moving <code>sqlite-wasm</code> to a Web Worker involves significant     architectural complexity:<ul> <li>Asynchronous message passing (request/response correlation).</li> <li>Build configuration changes to output separate worker bundles and manage   their loading.</li> <li>SharedArrayBuffer requirements for some high-performance modes (OPFS),   which require specific HTTP headers (<code>Cross-Origin-Opener-Policy</code>,   <code>Cross-Origin-Embedder-Policy</code>) that may not be available in all   deployment environments (e.g., simple GitHub Pages hosting without   configuration).</li> </ul> </li> <li>Scale: For typical usage (datasets of a few thousand rows), the     serialization and insertion time is negligible (&lt; 50ms).</li> <li>Simplicity: The synchronous API of <code>JsStorageDb</code> simplifies the code     significantly.</li> </ol>"},{"location":"docs/persistence_design/#optimization-batching","title":"Optimization: Batching","text":"<p>To mitigate blocking the main thread, we employ SQL Batching:</p> <ul> <li>Instead of executing thousands of individual <code>INSERT</code> statements, we construct   a single SQL string containing a transaction:   <code>sql   BEGIN TRANSACTION;   INSERT INTO ... VALUES ...;   INSERT INTO ... VALUES ...;   COMMIT;</code></li> <li>This reduces the overhead of crossing the JS-WASM boundary and ensures atomic   updates.</li> </ul>"},{"location":"docs/persistence_design/#future-work-worker-migration","title":"Future Work: Worker Migration","text":"<p>If dataset sizes grow significantly (e.g., &gt; 100k rows), blocking the main thread becomes a concern.</p> <p>Migration Plan:</p> <ol> <li>Create a dedicated worker namespace (e.g.,     <code>bb-web-ds-tools.workers.persistence</code>).</li> <li>Move the <code>persistence-fx</code> logic into that worker.</li> <li>Replace the <code>reg-fx</code> handlers to post messages to the worker instead of     calling DB functions directly.</li> <li>Implement a response handler event to update <code>app-db</code> upon completion.</li> </ol>"},{"location":"docs/r-integration/","title":"R Integration","text":"<p>The application integrates with the R runtime (WebR) and allows sharing datasets between the UI and R.</p>"},{"location":"docs/r-integration/#sharing-datasets","title":"Sharing Datasets","text":"<p>The application's global datasets are automatically synchronized with the R environment.</p>"},{"location":"docs/r-integration/#reading-datasets-in-r","title":"Reading Datasets in R","text":"<p>When you run R code, the current datasets from the \"Datasets\" view are available in the R global environment as a named list called <code>datasets</code>.</p> <p>The structure of <code>datasets</code> is a named list of data frames (or lists, depending on structure). You can access them by name:</p> <pre><code># Access the dataset named \"My Data\"\ndf &lt;- datasets[[\"My Data\"]]\nhead(df)\n</code></pre>"},{"location":"docs/r-integration/#updating-datasets-from-r","title":"Updating Datasets from R","text":"<p>Any modifications to the <code>datasets</code> variable in R are synchronized back to the application after the script finishes execution. This allows you to process data in R and view the results in the application.</p> <p>Example: Creating a new dataset</p> <pre><code># Create a new dataset in R\ndatasets[[\"Processed Data\"]] &lt;- mtcars\n</code></pre> <p>After running this code, a new dataset named \"Processed Data\" will appear in the \"Datasets\" view.</p> <p>Example: Modifying an existing dataset</p> <pre><code># Add a new column to an existing dataset\ndatasets[[\"My Data\"]]$new_col &lt;- datasets[[\"My Data\"]]$value * 2\n</code></pre> <p>The \"My Data\" dataset in the application will be updated with the new column.</p>"},{"location":"docs/r-integration/#technical-details","title":"Technical Details","text":"<ul> <li>Injection: Before executing user code, the application binds the <code>datasets</code> list to the R global environment.</li> <li>Synchronization: After execution, the application reads the <code>datasets</code> variable, converts it back to Clojure data, and updates the application state. It preserves dataset IDs for existing names but will generate new IDs for new names.</li> <li>Format: Data frames in R are converted to row-maps in the application. Ensure your R data structures are compatible (e.g., lists of atomic vectors).</li> </ul>"},{"location":"docs/visual_report/","title":"Visual Verification Report","text":""},{"location":"docs/visual_report/#overview","title":"Overview","text":"<p>A visual verification of the application was performed using automated Playwright scripts to capture screenshots of all primary views and sub-views. The goal was to assess layout stability, consistency of UI elements (buttons, panels), and overall visual coherence.</p>"},{"location":"docs/visual_report/#findings","title":"Findings","text":""},{"location":"docs/visual_report/#1-global-layout-instability-padding-margins","title":"1. Global Layout Instability (Padding &amp; Margins)","text":"<p>There is a significant inconsistency in how views handle the main content container's padding, causing content to \"jump\" when navigating between views.</p> <ul> <li>Full-Bleed Views:</li> <li><code>Code</code>, <code>Malli</code>, <code>Vega-Lite</code>, and <code>App DB</code> use the full height and width of     the content area (<code>h-full w-full</code>).</li> <li>These views often manage their own internal padding or use split-panes that     touch the edges of the screen.</li> <li>Padded Views:</li> <li><code>HoneySQL</code>, <code>Datasets</code>, and <code>Settings</code> apply a global padding (typically     <code>p-4</code> or <code>p-6</code>) to their main container.</li> <li><code>Gemma</code> uses a centered container approach (<code>container mx-auto px-4 py-4</code>).</li> <li><code>Changelog</code> uses a specific background color and centered layout     (<code>min-h-screen bg-[#3f3f3f] py-12 px-4</code>).</li> </ul> <p>Impact: The user experiences a jarring transition where the UI \"breathing room\" changes unpredictably. The dividing line between navigation and content shifts.</p>"},{"location":"docs/visual_report/#2-panel-alignment-split-views","title":"2. Panel Alignment &amp; Split Views","text":"<p>The application uses a mix of fixed-width columns and percentage-based split views, leading to misalignment of the primary \"editor/input\" areas across tools.</p> <ul> <li>Fixed Width: <code>Malli</code> and <code>HoneySQL</code> constrain their left panel   (input/controls) to <code>max-w-3xl</code>.</li> <li>Percentage Split: <code>Code</code> uses a 50/50 split (<code>w-1/2</code>). <code>App DB</code> uses a   25/75 split (<code>:ratio :1-3</code>). <code>Vega-Lite</code> uses a 50/50 split on large screens   (<code>2xl:w-1/2</code>).</li> </ul> <p>Impact: The vertical divider line jumps horizontally when switching between <code>Malli</code> -&gt; <code>Code</code> -&gt; <code>Vega-Lite</code>.</p>"},{"location":"docs/visual_report/#3-button-consistency","title":"3. Button Consistency","text":"<p>Button styling is fragmented, with local overrides defeating the purpose of a shared design system.</p> <ul> <li>Standard: <code>c/button</code> (used in <code>Gemma</code>, <code>HoneySQL</code>) appears to have default   padding/sizing.</li> <li>Malli Overrides: <code>Malli</code> defines a local <code>btn</code> function that forces   <code>!py-1 !px-3 !text-sm</code>, resulting in significantly smaller buttons than the   rest of the app.</li> <li>Vega-Lite: Uses a custom <code>tab-button</code> with <code>py-2 px-4</code>, different from   Malli's compressed style.</li> <li>App DB: Uses <code>c/button-xs</code> in some places, mixing sizes.</li> </ul> <p>Impact: Click targets vary in size and visual weight, reducing perceived polish.</p>"},{"location":"docs/visual_report/#4-navigation-headers","title":"4. Navigation &amp; Headers","text":"<ul> <li>Tabs: <code>Malli</code> and <code>Code</code> inject their sub-navigation tabs into the global   Top Bar (via <code>portal-to-top-bar</code>). <code>Vega-Lite</code> renders its tabs inside   the page content. This creates an inconsistent navigation model where \"view   modes\" sometimes appear in the chrome and sometimes in the canvas.</li> <li>Typography: Header usage is inconsistent.</li> <li><code>HoneySQL</code> uses <code>h3</code> with an icon.</li> <li><code>Datasets</code> uses <code>h2</code>.</li> <li><code>Gemma</code> uses <code>h3</code> with custom colors (<code>text-[#f0dfaf]</code>).</li> <li><code>Changelog</code> uses <code>h2</code> centered.</li> </ul>"},{"location":"docs/visual_report/#5-view-specific-observations","title":"5. View-Specific Observations","text":"<ul> <li>Gemma: The layout shifts to a centered \"container\" mode, unlike the   dashboard-style layout of other tools. While potentially intentional for a   \"chat\" interface, it breaks the continuity of the \"Tools\" suite.</li> <li>Vega-Lite: The internal border logic   (<code>border-b 2xl:border-b-0 2xl:border-r</code>) creates complex responsive behavior   that isn't mirrored in the <code>Code</code> view, despite similar split-pane   requirements.</li> </ul>"},{"location":"docs/visual_report/#recommendations","title":"Recommendations","text":"<ol> <li> <p>Standardize Main Container:</p> <ul> <li>Adopt a Full-Bleed default for all tool-style views (<code>Malli</code>, <code>Code</code>,   <code>HoneySQL</code>, <code>Vega-Lite</code>, <code>Datasets</code>).</li> <li>Move the <code>p-4</code> padding inside the layout components (e.g., inside the   specific panel that needs it) rather than on the root view container. This   ensures the edges (scrollbars, splitters) always align.</li> </ul> </li> <li> <p>Unify Button Styles:</p> <ul> <li>Refactor <code>bb-web-ds-tools.components.common</code> to include standard   <code>button-sm</code> and <code>button-md</code> variants.</li> <li>Remove the local <code>btn</code> definition in <code>malli.cljs</code> and replace with a   standard component.</li> </ul> </li> <li> <p>Consistent Split-Pane Ratios:</p> <ul> <li>Standardize on a set of split ratios. For code/preview tools, a default of   50/50 or <code>max-w-3xl</code> (fixed) should be chosen and applied consistently. If   <code>Malli</code> needs <code>max-w-3xl</code>, <code>HoneySQL</code> and <code>Code</code> should likely respect a   similar grid or max-width for the editor column.</li> </ul> </li> <li> <p>Navigation Consistency:</p> <ul> <li>Move <code>Vega-Lite</code> tabs to the Top Bar using <code>portal-to-top-bar</code> to match   <code>Malli</code> and <code>Code</code>. This reinforces the pattern that \"Sub-views live in   the Top Bar\".</li> </ul> </li> <li> <p>Typography System:</p> <ul> <li>Define standard <code>page-header</code> and <code>section-header</code> components in   <code>common.cljs</code> to enforce font-size (<code>text-xl</code> vs <code>text-2xl</code>) and color   consistency.</li> </ul> </li> </ol>"}]}